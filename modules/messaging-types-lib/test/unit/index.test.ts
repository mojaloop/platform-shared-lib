"use strict"

describe('example test', () => {

  beforeEach(async () => {
    // Setup
  })

  afterEach(async () => {
    // Cleanup
  })

  test('Messaging types module only, no tests', async () => {
    await expect(true)
  })
})
